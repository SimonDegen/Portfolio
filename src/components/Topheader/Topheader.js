import React, { Component } from 'react';

export class Topheader extends Component {
  render() {
    return(
      <p>Click on an emoji to view the emoji short name.</p>
    )
  }
}
export default Topheader; // Don’t forget to use export default!
